<% categories.each do |category| %>
    <%= link_to(category_path(category), :class => "dropdown") do
        simple_format "<h4>#{category.name} <span class='badge badge-default badge-pill pull-right'>#{category.manuals_count}</span></h4>"
      end
    %>
<% end %>